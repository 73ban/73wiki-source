# ymj8903668-droid External Backups

This directory keeps backup metadata and restore scripts for selected upstream repositories.

Goal:
- Keep a local, versioned record of important upstream projects.
- Make it easy to mirror them into the Mac mini later.
- Avoid depending on the upstream staying public forever.

See `backup-manifest.json` and `mirror-repos.ps1`.

